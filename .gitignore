node_modules
dist
*.tsbuildinfo
.DS_Store
.turbo
coverage

# An accidental `npm install` must not be committable.
package-lock.json

# Next.js artifacts, from the scratch apps used to verify the adapter.
.next

# Failure screenshots from the browser test project.
**/__screenshots__
.vitest-attachments

# Next 16 writes these into any app directory it runs in. Both configs set
# `agentRules: false`, so this is a belt on top of braces — a CLAUDE.md inside
# smoke/ or site/ would override this project's own instructions.
**/AGENTS.md
smoke/CLAUDE.md
site/CLAUDE.md

# Static export output from the smoke app and the documentation site.
smoke/out
site/out
next-env.d.ts

# Scratch project the README example type-check assembles.
.readme-check-*
