---
title: The page after
---

A second neighbour. `DocsPager` renders `null` when a page has neither, so on a
one-page fixture its three labels would have been "proven" against markup that
could not contain them.
